import { VElement } from '../../3rdparty/snabbdom/types';
declare function createContext(vnode: VElement): void;
declare const contextModule: {
    create: typeof createContext;
};
export default contextModule;
